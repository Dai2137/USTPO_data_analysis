# 設計書：意匠特許クロスモーダル検索システム

**目的:** 機能・用途を記述したテキストをクエリとして、意匠特許の画像DBを検索する

---

## 1. タスク定義

```
クエリ: テキスト（機能・用途の説明文）
DB:    画像のみ（全意匠特許の D00001.TIF）
出力:  Top-K 特許リスト
```

**Ground-truth ペア（評価用）:**

```
テキスト: "{title}. {caption}"   ← IMPACT の AI 生成キャプション
画像:     同一特許の D00001.TIF
```

---

## 2. アーキテクチャ

### 2-1. エンコーダ

| モダリティ | エンコーダ | 出力 | 状態 |
| --- | --- | --- | --- |
| 画像 | DINOv2-base | (B, 257, 768) パッチトークン列 | 初期実装 |
| テキスト | ModernBERT-base | (B, 128, 768) トークン列 | 初期実装 |

**懸念点・今後の試行錯誤:**

画像エンコーダは近年急速に進歩しており、DINOv2 が最善かは未確認。最終的な精度には影響する可能性がある。テキストエンコーダについても、Qwen3 Embedding 等の新しいモデルが有力候補。Phase 1 で動作確認後、エンコーダのアブレーションを行う。

| モダリティ | 候補エンコーダ | 備考 |
| --- | --- | --- |
| 画像 | DINOv2-base | 初期実装（まずはこれで動かす） |
| 画像 | SigLIP2 / InternViT / EVA-CLIP 等 | より強力な視覚エンコーダ候補 |
| テキスト | ModernBERT-base | 初期実装 |
| テキスト | Qwen3 Embedding | 多言語・高精度。将来的な有力候補 |

### 2-2. 表現の 3 成分分解（COrAL）

```
画像パッチ列 + テキストトークン列
    │
    ├─ FusionTransformer（CLS結合 + self-attention）
    │       → z_shared  (B, 512)   共有・相乗情報
    │
    ├─ 画像固有パス: Linear + AttentionPooling → MLP
    │       → z_img_uniq (B, 512)
    │
    └─ テキスト固有パス: Linear + AttentionPooling → MLP
            → z_txt_uniq (B, 512)
```

### 2-3. 損失関数

| 損失項 | 内容 |
| --- | --- |
| InfoNCE (shared) | z_shared 同士の対照学習 |
| InfoNCE (img_uniq) | z_img_uniq 同士の対照学習 |
| InfoNCE (txt_uniq) | z_txt_uniq 同士の対照学習 |
| 直交化 × 3 | shared↔img_uniq, shared↔txt_uniq, img_uniq↔txt_uniq |

### 2-4. 非対称マスキング（相乗情報の抽出）

```
View 1: 画像を部分マスク  + テキスト完全  → テキストから形状を推論
View 2: テキストを部分マスク + 画像完全   → 画像から機能を推論
```

カリキュラム（マスキング比率）: 5% → 35% → 55% → 75%（25エポックごと）

---

## 3. データ

| 項目 | 内容 |
| --- | --- |
| データセット | IMPACT（USPTO 意匠特許 2007〜2022年、約 455,100件） |
| 使用画像 | D00001.TIF（各特許の第1図面） |
| 使用テキスト | `{title}. {caption}` |
| 画像前処理 | 下8%クロップ（Fig.ラベル除去）、縦横比 >2.0 → 左半分、<0.45 → 上半分 |
| データ拡張（画像） | RandomResizedCrop(224, scale=0.4〜1.0) + RandomHorizontalFlip + CLIP正規化 |
| データ拡張（テキスト） | Word dropout (p=0.10) |

---

## 4. 実験フェーズ（スモールスタート）

### Phase 0 — スモークテスト（2022年の10〜100件、CPU）

- ハリボテエンコーダ（小さい MLP）で動作確認
- 環境: ローカル or Colab（GPU不要）
- 目的: パイプライン全体が壊れていないことの確認

### Phase 1 — 小規模実験（2022年のみ、約3.4万件、Colab T4）

- 本物のエンコーダで学習・評価
- 画像は Google Drive にマウント（ボトルネックになる場合はマルチスレッドで事前ダウンロード）
- 環境: Colab（VSCode + Jupyter から操作）
- 目的: Recall@K / MRR のベースライン値を得る

### Phase 2 — フルスケール（2007〜2022年、約45.5万件、GPUサーバー）

- Phase 1 で動作確認済みの設定でそのまま拡張
- 目的: 論文スケールの実験

---

## 5. 評価・比較実験

**① 従来の検索システムとの比較**（タイトル検索との差を示す）

| 手法 | 内容 |
| --- | --- |
| **タイトル検索（BM25）** | 製品名・ロカルノ分類のキーワードで照合。現行の意匠検索システムの主流 |
| **COrAL（提案手法）** | 機能文クエリ → 画像 DB クロスモーダル検索 |

→ 「タイトルだけの検索では取り出せない機能×形状の対応関係を、本手法は捉えられる」ことを示す。

**② アーキテクチャアブレーション**（提案手法の各設計要素の寄与を示す）

| 手法 | 内容 |
| --- | --- |
| **zero-shot CLIP** | fine-tuning なし。CLIP テキスト encoder → 画像 DB を直接検索 |
| **CLIP fine-tune（単純アライメント）** | IMPACT で InfoNCE のみで fine-tuning。共有・固有分解なし |
| **DeCUR ベース（2 成分）** | 共有・固有分解のみ。相乗なし |
| **COrAL（3 成分）** | 共有・固有・相乗分解 ＋ 非対称マスキング（提案手法） |


評価指標: Recall@1 / @5 / @10, MRR

---

## 6. 未解決事項・TODO

- [ ] `main_impact.py` → `.ipynb` 化（Colab 実行用）
- [ ] Phase 0 でハリボテエンコーダによるスモークテスト
- [ ] 2022年分を Google Drive にアップロード
- [ ] 林さんの Cloud Run 版ベクトル化関数の受け取りタイミング確認
- [ ] 「画像類似度 × テキスト類似度」のハイブリッドベースライン追加検討
- [ ] エンコーダアブレーション: 画像（DINOv2 vs SigLIP2 等）、テキスト（ModernBERT vs Qwen3 Embedding 等）
